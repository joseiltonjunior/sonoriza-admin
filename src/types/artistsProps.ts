export interface ArtistsResponseProps {
  id: string
  name: string
  photoURL: string
  musics: string[]
  like: number
}

export interface ArtistsEditDataProps {
  id: string
  photoURL: string
  name: string
}
